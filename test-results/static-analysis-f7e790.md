# Static Analysis Report: f7e7907

**Commit:** f7e7907 - test: Add allocation-tracking allocator test (requirement 4.6)
**Date:** 2026-02-23
**Tools:** cppcheck 2.19.0, scan-build (clang)

## cppcheck Results

**Errors:** 0
**Warnings:** 0
**Style findings:** 37 (all benign)

### Breakdown of style findings:
- `constParameterPointer` / `constParameterCallback` / `constVariablePointer`: 16 - Suggestions to add const qualifiers. Cannot change (libyaml API compatibility).
- `unreadVariable`: 10 - Variables assigned by STACK_DEL/QUEUE_DEL macros that set `.top`/`.end` after freeing. Macro artifact, not a bug.
- `shadowVariable`: 2 - Local `value` shadows outer `value` in error cleanup paths. Matches libyaml original code.
- `variableScope`: 4 - Suggestions to reduce variable scope. Style preference, not a bug.
- `knownConditionTrueFalse`: 2 - cppcheck thinks `yaml_emitter_check_empty_document` always returns false and `yaml_emitter_analyze_scalar` always succeeds. These are false positives from limited branch analysis (cppcheck itself notes "Limiting analysis of branches").
- `missingInclude`: 1 - config.h not found (expected, it's generated by cmake).
- `normalCheckLevelMaxBranches`: 2 - Informational, analysis was limited.

### Verdict: CLEAN
Zero errors, zero warnings. All findings are style-level and benign. No actionable bugs found.

## scan-build Results (from previous runs)

**Bugs found:** 0 real bugs, 1 false positive (dead code in writer.c surrogate pair path)

## Combined Assessment

Both static analysis tools report zero real bugs in the library source code.

Full cppcheck output saved to: `test-output/cppcheck-f7e790.txt`
